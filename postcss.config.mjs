/** Tailwind v4 is a single PostCSS plugin — no tailwind.config.js needed.
 *  Theme tokens live in app/globals.css under @theme. */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
